# ميزة تفعيل/تعطيل المنتجات وحد 10 منتجات نشطة

## نظرة عامة
تم إضافة نظام تفعيل/تعطيل المنتجات مع حد أقصى 10 منتجات نشطة للبائع.

## المفهوم الأساسي
- ✅ البائع يمكن أن يكون لديه **عدد غير محدود** من المنتجات
- ✅ فقط **10 منتجات كحد أقصى** يمكن أن تكون في حالة `active` (مفعلة)
- ✅ المنتجات الأخرى يمكن أن تكون: `inactive`, `pending_review`, أو `rejected`
- ✅ المنتجات المعطلة (`inactive`) **لا تظهر** في صفحة الاستكشاف
- ✅ المنتجات المعطلة **تظهر** في لوحة تحكم البائع مع علامة واضحة

## التغييرات المنفذة

### 1. Backend (Laravel)

#### أ) ProductController.php
**تم إضافة Method جديدة:**
```php
public function toggleStatus($id)
```

**الوظيفة:**
- تبديل حالة المنتج بين `active` و `inactive`
- التحقق من ملكية البائع للمنتج
- لا يمكن تبديل المنتجات في حالة `pending_review` أو `rejected` (يجب الموافقة عليها أولاً)
- عند التفعيل: التحقق من عدم تجاوز حد 10 منتجات نشطة
- إرجاع عدد المنتجات النشطة الحالي

**القيود:**
- المنتج يجب أن يكون في حالة `active` أو `inactive` فقط
- عند محاولة تفعيل منتج والعدد النشط = 10، يتم رفض الطلب برسالة خطأ

#### ب) routes/api.php
**تم إضافة Route جديد:**
```php
Route::post('seller/products/{id}/toggle-status', [ProductController::class, 'toggleStatus']);
```

#### ج) ExploreController.php
**تأكيد:**
- صفحة الاستكشاف تعرض فقط المنتجات بحالة `active` ✅
- السطر الموجود مسبقاً: `$query->where('status', 'active');`

### 2. Frontend (React)

#### أ) api.js
**تم إضافة Function جديدة:**
```javascript
toggleProductStatus: async (id) => {
  return apiFetch(`seller/products/${id}/toggle-status`, {
    method: 'POST',
  });
}
```

#### ب) DashboardGigs.jsx
**التغييرات:**

1. **Import جديد:**
   - `Switch` من `@/components/ui/switch`
   - أيقونات `Power`, `PowerOff` من `lucide-react`

2. **State جديد:**
   ```javascript
   const [togglingGigs, setTogglingGigs] = useState(new Set());
   const [activeProductsCount, setActiveProductsCount] = useState(0);
   ```

3. **Function جديدة:**
   ```javascript
   const handleToggleStatus = async (gigId, currentStatus) => {...}
   ```

4. **عداد المنتجات النشطة:**
   - يتم حساب عدد المنتجات النشطة تلقائياً
   - يظهر في أعلى الصفحة: "المنتجات النشطة: X من 10"

5. **زر التبديل (Switch):**
   - يظهر فقط للمنتجات في حالة `active` أو `inactive`
   - لا يظهر للمنتجات `pending_review` أو `rejected`
   - يتم تعطيل الزر أثناء التبديل (loading state)

6. **عرض الحالات:**
   - `active` → Badge أخضر "نشط"
   - `inactive` → Badge رمادي "معطّل"
   - `pending_review` → Badge خطوط "قيد المراجعة"
   - `rejected` → Badge أحمر "مرفوض" + عرض سبب الرفض

#### ج) CreateGigPage.jsx
**تحسين معالجة الأخطاء:**
- عرض رسالة واضحة عند محاولة تفعيل أكثر من 10 منتجات
- رسائل خطأ مفصلة للمستخدم

## سيناريوهات الاستخدام

### سيناريو 1: بائع لديه 10 منتجات نشطة
```
✅ المنتجات النشطة: 10/10
❌ لا يمكن تفعيل منتج آخر
✅ يمكن إضافة منتجات جديدة (ستكون في حالة pending_review)
✅ يمكن تعطيل أي منتج نشط
```

### سيناريو 2: بائع لديه 8 منتجات نشطة + 5 معطلة
```
✅ المنتجات النشطة: 8/10
✅ يمكن تفعيل منتجين آخرين من المعطلة
✅ إجمالي المنتجات: 13 (8 نشطة + 5 معطلة)
```

### سيناريو 3: منتج قيد المراجعة
```
⏳ حالة المنتج: pending_review
❌ لا يظهر زر التبديل
⏳ يجب انتظار موافقة الأدمن
✅ بعد الموافقة يصبح active ويظهر زر التبديل
```

### سيناريو 4: منتج مرفوض
```
❌ حالة المنتج: rejected
❌ لا يظهر زر التبديل
📝 يظهر سبب الرفض
✏️ يمكن التعديل وإعادة الإرسال للمراجعة
```

## تجربة المستخدم (UX)

### في لوحة التحكم (Dashboard)
1. **عرض عداد المنتجات:**
   ```
   خدماتي
   المنتجات النشطة: 7 من 10
   ```

2. **كروت المنتجات:**
   - Badge يوضح الحالة (نشط/معطّل/قيد المراجعة/مرفوض)
   - Switch لتفعيل/تعطيل المنتجات المؤهلة
   - نص توضيحي بجانب الـSwitch

3. **رسائل Toast:**
   - عند التفعيل: "تم تفعيل المنتج بنجاح - المنتجات النشطة: X من 10"
   - عند التعطيل: "تم تعطيل المنتج بنجاح - المنتجات النشطة: X من 10"
   - عند الوصول للحد: "لقد وصلت للحد الأقصى من المنتجات المفعلة (10 منتجات)"

### في صفحة الاستكشاف
- تظهر فقط المنتجات النشطة (`active`)
- المنتجات المعطلة لا تظهر نهائياً
- المنتجات قيد المراجعة لا تظهر
- المنتجات المرفوضة لا تظهر

## API Responses

### نجاح التفعيل:
```json
{
  "message": "تم تفعيل المنتج بنجاح",
  "product": {...},
  "active_count": 8,
  "total_slots": 10
}
```

### فشل التفعيل (الحد الأقصى):
```json
{
  "message": "لقد وصلت للحد الأقصى من المنتجات المفعلة (10 منتجات). يرجى تعطيل منتج آخر أولاً.",
  "active_count": 10,
  "limit": 10
}
```

### محاولة تبديل منتج قيد المراجعة:
```json
{
  "message": "لا يمكن تفعيل/تعطيل هذا المنتج. يجب أن تتم الموافقة عليه من الإدارة أولاً.",
  "current_status": "pending_review"
}
```

## الأمان (Security)

1. ✅ التحقق من ملكية البائع للمنتج
2. ✅ التحقق من حالة المنتج قبل التبديل
3. ✅ عد المنتجات النشطة server-side (لا يمكن التلاعب)
4. ✅ رسائل خطأ واضحة بدون كشف بيانات حساسة
5. ✅ تسجيل الدخول مطلوب (auth:sanctum)

## الملفات المعدلة

### Backend:
1. `backend/app/Http/Controllers/Api/ProductController.php` - إضافة toggleStatus method
2. `backend/routes/api.php` - إضافة route جديد

### Frontend:
1. `src/lib/api.js` - إضافة toggleProductStatus function
2. `src/components/dashboard/DashboardGigs.jsx` - إضافة UI للتبديل والعداد
3. `src/components/dashboard/CreateGigPage.jsx` - تحسين معالجة الأخطاء

## اختبار الميزة

### خطوات الاختبار:
1. سجل دخول كبائع
2. أضف 10 منتجات وفعّلها جميعاً (أو انتظر موافقة الأدمن)
3. حاول تفعيل منتج 11 → يجب أن يظهر خطأ
4. عطّل منتج واحد → يجب أن يصبح العداد 9/10
5. فعّل منتج آخر → يجب أن يصبح العداد 10/10
6. تأكد أن المنتجات المعطلة لا تظهر في صفحة الاستكشاف
7. تأكد أن المنتجات المعطلة تظهر في لوحة التحكم

## ملاحظات إضافية

- المنتجات في حالة `pending_review` لا تُحتسب ضمن الحد الأقصى للتفعيل
- المنتجات المرفوضة (`rejected`) لا تُحتسب ضمن الحد
- يمكن للبائع أن يكون لديه مثلاً: 10 نشطة + 5 معطلة + 3 قيد المراجعة + 2 مرفوضة = 20 منتج إجمالي
- عند حذف منتج نشط، يمكن تفعيل منتج آخر مباشرة
